<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jamieholdstock.crossworddictionary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.tutorial.TheBasicsActivity" />
        <activity android:name=".activities.tutorial.TutorialMenuActivity" />
        <activity android:name=".activities.tutorial.DevicesActivity" />
        <activity android:name=".activities.SearchActivity" />
    </application>

</manifest>
